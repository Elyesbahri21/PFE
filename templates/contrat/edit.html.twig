{% extends 'base.html.twig' %}

{% block title %}Modifier Contrat{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Modifier Contrat</h1>

        <div class="card">
            <div class="card-body">
                {{ include('contrat/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_contrat_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        <div class="mt-3">
                        <a class="btn btn-outline-secondary" href="javascript:void(0);" onclick="deleteContrat({{ contrat.id }});">Supprimer</a>      
        </div>
    </div>
    
{% endblock %}
