
    {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.date) }}{% if form.date.vars.required %}<span class="text-danger">*</span>{% endif %}
            {{ form_widget(form.date) }}
            {{ form_errors(form.date) }}
        </div>
        <div class="form-group">
            {{ form_label(form.type) }}{% if form.type.vars.required %}<span class="text-danger">*</span>{% endif %}
            {{ form_widget(form.type) }}
            {{ form_errors(form.type) }}
        </div>
        <div class="form-group">
            {{ form_label(form.description) }}{% if form.description.vars.required %}<span class="text-danger">*</span>{% endif %}
            {{ form_widget(form.description) }}
            {{ form_errors(form.description) }}
        </div>
        <div class="form-group">
            {{ form_label(form.pv) }}{% if form.pv.vars.required %}<span class="text-danger">*</span>{% endif %}
            {{ form_widget(form.pv) }}
            {{ form_errors(form.pv) }}
        </div>
        <div class="form-group">
            {{ form_label(form.contrat) }}{% if form.contrat.vars.required %}<span class="text-danger">*</span>{% endif %}
            {{ form_widget(form.contrat) }}
            {{ form_errors(form.contrat) }}
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    {{ form_end(form) }}
